server:
  port: 8011
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: pet-provider

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    first:
    initial-size: 1
    max-active: 5
    password: root
    url: jdbc:mysql://192.168.1.126:3306/pet?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
    username: root
    jpa:
    database: mysql

  redis:
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
mybatis:
  mapper-locations: classpath:mapper/*.xml

